# bizwy_backend

Backend API for Bizwy.

## Setup

1. Copy `.env.example` to `.env` and fill in your values.
2. Install dependencies: `pip install -r requirements.txt`
3. Run with Docker: `docker compose up` or see `API.md` for endpoints.
